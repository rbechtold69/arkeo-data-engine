# Config directory

This placeholder keeps the `config/` directory in source control so Docker builds can copy it into the image. Runtime files can be mounted or written inside the container at `/app/config/` if needed later.
