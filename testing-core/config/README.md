# Config directory placeholder

This container is static-only and doesn’t use runtime configs. The directory exists so host mounts like `-v ~/testing-core/config:/app/config` succeed without extra setup. Feel free to drop test notes here if you want to verify volume mounts.
